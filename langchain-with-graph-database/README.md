# Langchain and Graph Database

This directory contains a sample implementation of an agentic AI system using LangChain to qury and perform semantic reasoning with a graph database. TThe project with setup a sample ontology for graph data along with synthetic data and demostrate querying the dataset for reasoning.